# Test Environment Configuration
TEST_ENV=test
BASE_URL=http://localhost:3000
API_URL=http://localhost:3000/api/v1

# Suppress logs during tests (set to 'true' to silence)
SUPPRESS_TEST_LOGS=false

# OpenAI API (use test key or mock)
OPENAI_API_KEY=test-key-mock

# Mapy.cz API (use test key or mock)
MAPY_CZ_API_KEY=test-key-mock

# Supabase (use test instance)
SUPABASE_URL=https://test.supabase.co
SUPABASE_KEY=test-key-mock
